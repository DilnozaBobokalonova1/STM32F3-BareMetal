#include "stm32f3xx.h"

#define GPIOEEN					(1U<<21)
#define PIN15					(1U<<15)
#define LED_PIN					PIN15


int main() {
	RCC->AHBENR |= GPIOEEN;
	GPIOE->MODER |= (1U<<30);
	GPIOE->MODER &= ~(1U<<31);

	while(1) {
		GPIOE->ODR ^= LED_PIN;
		for (int i = 0; i < 1000000; i++) {}
	}
}
